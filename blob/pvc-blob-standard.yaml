apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-blob-standard
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  volumeName: pv-blob-standard
  storageClassName: azureblob-fuse-premium